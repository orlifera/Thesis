#import "../config/variables.typ": myDegree, myName, myTime, myTitle

#set page(numbering: none)
#set par(leading: 0.55em, justify: true)
#set text(font: "New Computer Modern", size: 10pt)
#show par: set block(spacing: 0.55em)
#show heading: set block(above: 1.4em, below: 1em)


#align(right, [
  A Nonno Enzo, che mi ha insegnato a vivere.

  Ai miei genitori, che mi hanno reso l'uomo che sono.

  Al mare, che mi ha insegnato il rispetto, perché egli non perdona.
  #v(6pt)
  #sym.dash#sym.dash#sym.dash
])

#align(center + horizon, text(style: "italic")[
  Tenetevi il mondo e lasciatemi il mare
])
